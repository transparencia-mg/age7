fields:
  - name: id_estado
    title: Estado - Chave
    type: integer
    format: default
    bareNumber: true
    constraints:
      required: true
      unique: false
  - name: nome
    title: Estado - Descrição
    type: string
    format: default
    description: |
      Estado ao qual pertence o município de origem ou destino do servidor ou colaborador eventual em viagem a trabalho.
    constraints:
      required: true
missingValues:
  - ''
primaryKey:
  - id_estado
