fields:
  - name: id_cargo
    title: Cargo - Chave
    type: integer
    format: default
    bareNumber: true
    constraints:
      required: true
      unique: false
  - name: nome
    title: Cargo - Descrição
    type: string
    format: default
    description: |
      Cargo público ocupado pelo servidor que realizou a viagem.
    constraints:
      required: true
missingValues:
  - ''
primaryKey:
  - id_cargo
