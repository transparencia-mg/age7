fields:
  - name: id_municipio
    title: Município - Chave
    type: integer
    format: default
    bareNumber: true
    constraints:
      required: true
      unique: false
  - name: cd_municipio
    title: Município - Código
    type: integer
    format: default
    bareNumber: true
    constraints:
      required: true
      unique: false
  - name: nome
    title: Município
    type: string
    format: default
    description: |
      - > Nome do Município da destinação do bem ou serviço que resulta da ação.
missingValues:
  - ''
primaryKey:
  - id_municipio
